<ul>
  <li>
    <% decks.each do |deck| %>
      <div> <%= link_to deck.name, deck %> </div>
      <div>
        Tags:
        <% deck.tags.each do |tag| %>
          <%= link_to "#{tag.name}", tags_path(tag) %>
        <% end %>
      </div>
    <% end %>
  </li>
</ul>
