<%= form_for :guess, url: guess_url, method: :post, remote: true do |f| %>
  <%= f.hidden_field :current_card %>
  <%= f.hidden_field :next_card %>
  <%= f.hidden_field :deck_id %>
  <%= f.submit 'Next Card' %>
<% end %>
