<%= @card_last_id %>

<h2><%= @deck.name %>:</h2>

<div id='guess_container'>
  <%= render 'guesses/guess', card: @card, num_guesses: @num_guesses, last_card: @card_last_id %>
</div>

<script>
  var index = 0;
  var card_ids = <%= @card_ids %>;
  var deck_id = <%= @deck.id %>;
  var last_card_id = <%= @card_last_id %>;
  var guess_bank = [];

  $( document ).ready(function() {
    $(document).on('submit', '#guess_submission', function() {
      var guess_value = $('#guess_guess').val();
      guess_bank.push(guess_value);
    });
  });
</script>
